# Reviews-from-JSON-w-ExpressJS-jQuery

Took reviews from a local JSON file. Fetched them with jQuery.getJSON() and used Swiper to make slide show with reviews in EJS.

Font Awesome
jQuery
ExpressJS
Swiper
